rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Firebase Authentication을 붙이기 전에는 브라우저의 모든 읽기·쓰기를 차단합니다.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
